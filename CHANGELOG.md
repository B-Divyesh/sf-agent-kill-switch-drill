# Changelog

## Unreleased

- Add a bounded timeout to every allowlisted command and record timed-out
  checks as failed incident-card checkpoints.
- Add `demo`, which runs the bundled harmless sample in a temporary directory.
- Repair support-unlock gating, dark-theme contrast, touch targets, package
  contents, and static-site headers and 404 handling.

## 0.1.0 — 2026-08-28

- First release: allowlisted staged drills, dry-run default, verified incident cards, JSON output, and a local static documentation site.
